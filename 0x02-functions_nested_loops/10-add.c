#include "main.h"

/**
 * add - function that adds two integers
 * @a: holds the integer value
 * @b: holds the other integer value
 * Return: Always (0) Success
 */

int add(int a, int b)
{
	return (a + b);
}
