#include "main.h"

/**
 * _strlen - function that return the length of string
 *
 * @s: char type pointer
 *
 * Return: string length 
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	return (i);
}
