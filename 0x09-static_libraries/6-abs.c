#include "main.h"

/**
 * _abs - program computes absolute value of an integer
 * @i: holds the number value
 * Return: return under some conditions
 */

int _abs(int i)
{
	if (i < 0)
	{
		return (i * -1);
	}

	return (i);
}
